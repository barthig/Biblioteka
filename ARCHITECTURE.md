# 🏗️ Architektura Systemu Bibliotecznego

## 📊 Przegląd Wysokiego Poziomu

```
┌─────────────────────────────────────────────────────────────────┐
│                         UŻYTKOWNIK                               │
│                      (Przeglądarka Web)                          │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP/HTTPS
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                    FRONTEND (React SPA)                          │
│                   http://localhost:5173                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐         │
│  │   Pages     │  │  Components  │  │    Contexts    │         │
│  │  (12 stron) │  │  (14 komp.)  │  │  - Auth        │         │
│  │             │  │              │  │  - Cache       │         │
│  └──────┬──────┘  └──────┬───────┘  └────────┬───────┘         │
│         │                │                   │                  │
│         └────────────────┼───────────────────┘                  │
│                          │                                      │
│                  ┌───────▼────────┐                            │
│                  │   Services     │                            │
│                  │  (5 serwisów)  │                            │
│                  │  - book        │                            │
│                  │  - loan        │                            │
│                  │  - reservation │                            │
│                  │  - user        │                            │
│                  │  - announcement│                            │
│                  └───────┬────────┘                            │
│                          │                                      │
│                  ┌───────▼────────┐                            │
│                  │    api.js      │                            │
│                  │  (HTTP Wrapper)│                            │
│                  │  + JWT Token   │                            │
│                  └───────┬────────┘                            │
└──────────────────────────┼───────────────────────────────────────┘
                           │
                           │ REST API (JSON)
                           │ Authorization: Bearer {JWT}
                           │
┌──────────────────────────▼───────────────────────────────────────┐
│                   BACKEND (Symfony 6)                            │
│                  http://localhost:8000                           │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              API Layer (Controllers)                    │    │
│  │  /api/books, /api/loans, /api/reservations, /api/auth  │    │
│  └─────────────────────────┬──────────────────────────────┘    │
│                            │                                     │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │           Security Layer                                │    │
│  │  - ApiAuthSubscriber (JWT + API Secret)                │    │
│  │  - JwtService (HS256)                                   │    │
│  └─────────────────────────┬──────────────────────────────┘    │
│                            │                                     │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │          Business Logic (Services)                      │    │
│  │  - BookService                                          │    │
│  │  - LoanService                                          │    │
│  │  - ReservationService                                   │    │
│  │  - BookCacheService                                     │    │
│  │  - StatisticsCacheService                               │    │
│  └─────────────────────────┬──────────────────────────────┘    │
│                            │                                     │
│  ┌─────────────────────────▼──────────────────────────────┐    │
│  │            Data Layer (Doctrine ORM)                    │    │
│  │  Entities: Book, Author, Category, BookCopy,           │    │
│  │           Loan, Reservation, User, Fine                 │    │
│  └─────────────────────────┬──────────────────────────────┘    │
└──────────────────────────┼─────────────────────────────────────┘
                           │
                           │ PDO/PostgreSQL Protocol
                           │
┌──────────────────────────▼───────────────────────────────────────┐
│                  DATABASE (PostgreSQL 15)                        │
│                    localhost:5432                                │
├──────────────────────────────────────────────────────────────────┤
│  Tables:                                                         │
│  - book, author, category, book_copy                            │
│  - loan, reservation, fine                                       │
│  - user, role                                                    │
│  - announcement, digital_asset                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Przepływ Danych (Data Flow)

### Przykład: Wypożyczenie Książki

```
┌─────────────┐
│ Użytkownik  │
│ klika       │
│ "Wypożycz"  │
└──────┬──────┘
       │
       ▼
┌──────────────────────────┐
│ LoanCard Component       │
│ onClick={handleBorrow}   │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ loanService.createLoan() │
│ (bookId, userId)         │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ api.js (apiFetch)        │
│ POST /api/loans          │
│ + JWT Token              │
└──────┬───────────────────┘
       │
       │ HTTP Request
       ▼
┌──────────────────────────┐
│ ApiAuthSubscriber        │
│ Weryfikacja JWT          │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ LoanController::create() │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ LoanService              │
│ - Sprawdź dostępność     │
│ - Sprawdź limity         │
│ - Utwórz wypożyczenie    │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Doctrine ORM             │
│ - Pobierz BookCopy       │
│ - Utwórz Loan entity     │
│ - Zapisz do bazy         │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ PostgreSQL               │
│ INSERT INTO loan         │
└──────┬───────────────────┘
       │
       │ Response (JSON)
       ▼
┌──────────────────────────┐
│ Frontend                 │
│ - Aktualizuj UI          │
│ - Pokaż sukces           │
│ - Invalidate cache       │
└──────────────────────────┘
```

---

## 🧩 Warstwa Frontendowa (Szczegółowo)

```
frontend/src/
│
├── pages/ ────────────────┐
│   ├── Dashboard.jsx      │  Prezentacja
│   ├── Books.jsx          │  (Widoki)
│   ├── MyLoans.jsx        │
│   └── ...                │
│                          │
├── components/ ───────────┤
│   ├── LoanCard.jsx       │  UI Components
│   ├── Modal.jsx          │  (Reusable)
│   ├── Pagination.jsx     │
│   └── ...                │
│                          │
├── services/ ─────────────┤
│   ├── bookService.js     │  API Layer
│   ├── loanService.js     │  (Business Logic)
│   ├── userService.js     │
│   └── ...                │
│                          │
├── api.js ────────────────┤  HTTP Client
│                          │  (axios wrapper)
│                          │
└── context/ ──────────────┘
    ├── AuthContext.jsx       Global State
    └── ResourceCacheContext  (React Context)
```

### Zależności między warstwami:

```
Pages → Components → Services → api.js → Backend
  ↓         ↓            ↓
Context ────┴────────────┘
```

---

## 🔐 Przepływ Autoryzacji

```
┌─────────────────┐
│ Login Form      │
│ email/password  │
└────────┬────────┘
         │
         ▼
┌────────────────────────┐
│ AuthContext.login()    │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ POST /api/auth/login   │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ AuthController::login()│
│ - Sprawdź hasło        │
│ - Wygeneruj JWT        │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ Response:              │
│ {                      │
│   token: "eyJ...",     │
│   user: {...}          │
│ }                      │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ localStorage.setItem() │
│ 'token', jwt           │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ Wszystkie requesty:    │
│ Authorization: Bearer  │
│ {token}                │
└────────────────────────┘
```

---

## 📦 Struktura Bazy Danych

```
┌──────────────┐       ┌──────────────┐
│    Author    │       │   Category   │
├──────────────┤       ├──────────────┤
│ id (PK)      │       │ id (PK)      │
│ name         │       │ name         │
│ bio          │       │ description  │
└──────┬───────┘       └──────┬───────┘
       │                      │
       │ 1:N                  │ N:M
       │                      │
┌──────▼──────────────────────▼───────┐
│            Book                      │
├──────────────────────────────────────┤
│ id (PK)                              │
│ title                                │
│ isbn                                 │
│ author_id (FK)                       │
│ published_year                       │
│ description                          │
└──────┬───────────────────────────────┘
       │ 1:N
       │
┌──────▼───────┐
│  BookCopy    │
├──────────────┤
│ id (PK)      │
│ book_id (FK) │
│ status       │
│ location     │
└──────┬───────┘
       │ 1:N
       │
       ├─────────────┬──────────────┐
       │             │              │
┌──────▼──────┐ ┌───▼────────┐ ┌──▼──────────┐
│    Loan     │ │ Reservation│ │DigitalAsset │
├─────────────┤ ├────────────┤ ├─────────────┤
│ id (PK)     │ │ id (PK)    │ │ id (PK)     │
│ copy_id(FK) │ │ book_id(FK)│ │ book_id(FK) │
│ user_id(FK) │ │ user_id(FK)│ │ type        │
│ borrowed_at │ │ created_at │ │ file_path   │
│ due_date    │ │ status     │ │ mime_type   │
│ returned_at │ │ expires_at │ └─────────────┘
│ extensions  │ └────────────┘
└─────────────┘

┌──────────────┐
│     User     │
├──────────────┤
│ id (PK)      │
│ email        │
│ password     │
│ name         │
│ roles (JSON) │
└──────────────┘
```

---

## 🔄 Cache Strategy

```
Frontend Cache (ResourceCacheContext):
┌────────────────────────────┐
│ Key-Value Store            │
│ TTL: 60 seconds            │
│                            │
│ books:* ────────► Books    │
│ loans:* ────────► Loans    │
│ reservations:* ─► Reserv.  │
└────────────────────────────┘

Backend Cache (Symfony Cache):
┌────────────────────────────┐
│ CacheInterface             │
│ TTL: Configurable          │
│                            │
│ book_stats ─────► Stats    │
│ popular_books ──► Books    │
│ user_stats ─────► Stats    │
└────────────────────────────┘
```

---

## 🎨 UI Component Hierarchy

```
App
├── Navbar
│   ├── NavLink (Dashboard)
│   ├── NavLink (Books)
│   ├── NavLink (Announcements)
│   └── NavLink (MyLoans)
│
└── Routes
    ├── Dashboard
    │   ├── Hero
    │   ├── StatCard × 4
    │   ├── AnnouncementCard × 3
    │   └── BookItem × 12
    │
    ├── Books
    │   ├── SearchBar
    │   ├── FilterPanel
    │   ├── BookItem × N
    │   └── Pagination
    │
    ├── MyLoans
    │   ├── LoanCard × N
    │   └── Pagination
    │
    ├── Announcements
    │   ├── AnnouncementCard × N
    │   └── Pagination
    │
    └── BookDetails
        ├── BookInfo
        ├── ActionButtons
        └── Modal (Borrow/Reserve)
```

---

## 🔌 API Endpoints (Backend)

```
Authentication:
POST   /api/auth/login       - Logowanie (JWT)
POST   /api/auth/register    - Rejestracja

Books:
GET    /api/books            - Lista książek
GET    /api/books/{id}       - Szczegóły książki
GET    /api/books/search     - Wyszukiwanie
GET    /api/books/recommended- Polecane
GET    /api/books/popular    - Popularne
GET    /api/books/new        - Nowości
GET    /api/books/filters    - Dostępne filtry

Loans:
GET    /api/loans            - Moje wypożyczenia
POST   /api/loans            - Nowe wypożyczenie
PUT    /api/loans/{id}/return- Zwrot
PUT    /api/loans/{id}/extend- Przedłużenie
GET    /api/loans/stats      - Statystyki

Reservations:
GET    /api/reservations     - Moje rezerwacje
POST   /api/reservations     - Nowa rezerwacja
DELETE /api/reservations/{id}- Anuluj
PUT    /api/reservations/{id}/fulfill - Zrealizuj

Users:
GET    /api/me               - Mój profil
PUT    /api/me               - Aktualizuj profil
POST   /api/me/password      - Zmiana hasła
GET    /api/favorites        - Ulubione
POST   /api/favorites        - Dodaj ulubiony
DELETE /api/favorites/{id}   - Usuń ulubiony

Announcements:
GET    /api/announcements    - Lista ogłoszeń
GET    /api/announcements/{id}- Pojedyncze
POST   /api/announcements    - Utwórz (admin)
PUT    /api/announcements/{id}- Aktualizuj
DELETE /api/announcements/{id}- Usuń
POST   /api/announcements/{id}/publish - Opublikuj
POST   /api/announcements/{id}/archive - Archiwizuj
```

---

## 🚀 Deployment Architecture (Production)

```
┌─────────────────────────────────────────┐
│         Load Balancer (nginx)           │
│         SSL/TLS Termination             │
└───────────┬─────────────────────────────┘
            │
            ├─────────────┬───────────────┐
            │             │               │
┌───────────▼──┐  ┌───────▼──┐  ┌────────▼──┐
│  Frontend    │  │  Frontend│  │  Frontend │
│  (nginx +    │  │  Instance│  │  Instance │
│   React)     │  │    #2    │  │    #3     │
└──────────────┘  └──────────┘  └───────────┘

┌──────────────────────────────────────────┐
│       API Gateway / Reverse Proxy        │
└───────────┬──────────────────────────────┘
            │
            ├─────────────┬────────────────┐
            │             │                │
┌───────────▼──┐  ┌───────▼──┐  ┌─────────▼──┐
│  Backend     │  │  Backend │  │  Backend   │
│  (Symfony +  │  │  Instance│  │  Instance  │
│   PHP-FPM)   │  │    #2    │  │    #3      │
└──────┬───────┘  └──────┬───┘  └──────┬─────┘
       │                 │             │
       └─────────────────┼─────────────┘
                         │
           ┌─────────────▼──────────────┐
           │  PostgreSQL Master         │
           │  (Read/Write)              │
           └─────────────┬──────────────┘
                         │ Replication
           ┌─────────────▼──────────────┐
           │  PostgreSQL Replica        │
           │  (Read Only)               │
           └────────────────────────────┘

┌──────────────────────────────────────────┐
│         Cache Layer (Redis)              │
│  - Session Storage                       │
│  - API Response Cache                    │
└──────────────────────────────────────────┘
```

---

## 📈 Monitoring & Observability

```
┌─────────────────────────────────────┐
│           Application               │
│    Frontend + Backend               │
└───────────┬─────────────────────────┘
            │
            ├──► Logs ───────────► ELK Stack
            │                      (Elasticsearch,
            │                       Logstash, Kibana)
            │
            ├──► Metrics ────────► Prometheus
            │                      + Grafana
            │
            ├──► Errors ─────────► Sentry
            │
            └──► Tracing ────────► Jaeger
```

---

## ✅ Podsumowanie

### Kluczowe cechy architektury:

1. **Separacja warstw** - Frontend/Backend całkowicie oddzielone
2. **RESTful API** - Komunikacja przez JSON
3. **JWT Authentication** - Bezpieczna autoryzacja
4. **Service Layer** - Czysta logika biznesowa
5. **ORM** - Doctrine dla warstwy danych
6. **Component-based UI** - React komponenty wielokrotnego użytku
7. **Caching** - Multi-level cache (frontend + backend)
8. **Responsywność** - Mobile-first design
9. **Skalowalność** - Możliwość horizontal scaling
10. **Bezpieczeństwo** - JWT + API Secret + Role-based access

### Wzorce projektowe:

- **MVC** (Model-View-Controller) - Symfony
- **Service Layer** - Oddzielenie logiki biznesowej
- **Repository Pattern** - Doctrine Repositories
- **Context API** - React state management
- **Compound Components** - Złożone komponenty UI
- **HOC** (Higher-Order Components) - RequireRole
- **Container/Presenter** - Smart/Dumb components

---

**Architektura zapewnia:**
- ✅ Łatwość w utrzymaniu
- ✅ Skalowalność
- ✅ Bezpieczeństwo
- ✅ Wydajność
- ✅ Testowalność
